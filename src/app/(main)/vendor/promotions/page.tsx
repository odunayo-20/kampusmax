import { Megaphone } from "lucide-react";
import { PlaceholderPage } from "@/components/vendor-dashboard/PlaceholderPage";

export default function PromotionsPage() {
  return (
    <PlaceholderPage
      title="Promotions"
      icon={Megaphone}
      description="Create discounts, coupons, and campaigns for your store. This module ships in a later release."
    />
  );
}