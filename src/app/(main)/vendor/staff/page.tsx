import { UsersRound } from "lucide-react";
import { PlaceholderPage } from "@/components/vendor-dashboard/PlaceholderPage";

export default function StaffPage() {
  return (
    <PlaceholderPage
      title="Staff"
      icon={UsersRound}
      description="Manage teammates and their permissions on your store. This module ships in a later release."
    />
  );
}