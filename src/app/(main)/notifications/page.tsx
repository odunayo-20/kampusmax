import { PageContainer } from "@/components/layout";

export default function NotificationsPage() {
  return (
    <PageContainer className="text-center py-16">
      <p className="text-4xl mb-3">Notifications</p>
      <h1 className="text-lg font-semibold text-kampmax-text mb-1">Notifications</h1>
      <p className="text-sm text-kampmax-text-secondary">
        Coming in Phase 2 — Order updates, messages, and community alerts
      </p>
    </PageContainer>
  );
}
